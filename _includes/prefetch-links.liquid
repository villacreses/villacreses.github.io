{%- assign feature_pages = site.html_pages 
  | where_exp: "pl", "pl != page.permalink and pl != '/404.html' and pl != '/qr'"
  | map: "permalink" 
-%}
{% for pl in feature_pages %}
  <link rel="prefetch" href="{{pl}}" as="document">{% endfor %}