{%- capture prefetch -%}
{%- for p in site.html_pages -%}
  {% if p.permalink != page.permalink and p.permalink != '/404.html' and p.permalink != '/qr' %}
  <link rel="prefetch" href="{{ p.permalink }}" as="document">{% endif %}
{%- endfor -%}
{%- endcapture -%}
{{ prefetch | strip }}