---
layout: base
---
  <main class="compact">
    <header class="standard">
      <h1>{{page.title}}</h1>
      <p class="prose">
        {{page.heading_description}}
      </p>
    </header>
    {{ content }}
  </main>